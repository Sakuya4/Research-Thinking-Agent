"""
RTA package.

Design goals (firmware-like discipline):
- deterministic I/O boundaries per stage
- run-based artifact generation (reproducible debugging)
- structured event logs (JSONL) with stage attribution
"""
